// Re-exported from the shared workspace package so main/preload/renderer imports
// (`../shared/ipc`) don't need to change; the canonical source lives in packages/shared.
export * from '@interviewhelper/shared'

